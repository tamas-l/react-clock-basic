apiVersion: v1
kind: Service
metadata:
    name: "tlak8s"
    labels:
        app: "tlak8s"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "tlak8s"